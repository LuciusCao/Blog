import React from 'react';
import styled from 'styled-components';

const LoadingIndicator = styled.p`
  color: white;
`

export default () => {
  <LoadingIndicator>
    loading now
  </LoadingIndicator>
}
